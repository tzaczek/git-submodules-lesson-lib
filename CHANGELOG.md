# Changelog

## 1.1.0

- Adds `Greeter.Version` (`"1.1.0"`) and `Greeter.Farewell(name)` (`Goodbye, {name}!`).
- `Greeter.Greet(name)` now returns two lines: `Hello, {name}! (shared-lib 1.1.0)` and `Goodbye, {name}!`.

## 1.0.0

- `Greeter.Greet(name)` returns `Hello, {name}! (shared-lib 1.0.0)`.
